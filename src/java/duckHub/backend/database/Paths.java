package duckHub.backend.database;

import java.io.File;
import java.io.FileDescriptor;

public interface Paths {
    String USERS_DATABASE_PATH = "src" + File.separator + "Resources" + File.separator + "duckHub" + File.separator + "backend" + File.separator;

}
